import React from 'react'

function Input({name,mail}) {
    return (
        <div>
           
        <h1>{mail}</h1>
        <h2>{name}</h2>
        </div>
    )
}

export default Input
